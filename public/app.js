const state={apiBase:'',discord:'https://discord.gg/FQzUhm9xDu',form:'https://forms.gle/49LUgAW7TmLzFhmK7',googleClientId:'',projects:[],players:[],category:'All',user:null,googleProfile:null};
const $=s=>document.querySelector(s);
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const icons={'All':'fa-layer-group','Add-ons':'fa-puzzle-piece','Mods':'fa-wrench','Resource Packs':'fa-images','Behavior Packs':'fa-gears','Worlds':'fa-earth-americas','Maps':'fa-map','Skin Packs':'fa-shirt','Shaders':'fa-wand-magic-sparkles','Servers':'fa-server','Tutorials':'fa-book-open'};
const categories=[['All','Everything'],['Add-ons','Gameplay additions'],['Mods','Community tools'],['Resource Packs','Textures & visuals'],['Behavior Packs','Gameplay logic'],['Worlds','Downloadable worlds'],['Maps','Adventure & PvP'],['Skin Packs','Player skins'],['Shaders','Visuals'],['Servers','Communities'],['Tutorials','Guides']];
const faqs=[
['Do I need an account to browse?','No. Browsing the site, guides, tier explanations and public community videos does not require sign-in.'],
['Why does the site use Google sign-in?','Google sign-in gives the community one account identity without asking this site to store a password. Google returns a signed ID token which the backend verifies.'],
['What profile information is required after sign-in?','You can choose the display name used by the community and optionally add a YouTube channel and Discord handle. Your Google profile photo is not stored by this site.'],
['Can I use the site without adding a YouTube channel?','Yes. The YouTube field is optional.'],
['Can I use the site without adding Discord?','Yes. Discord is optional on your profile, although some community and staff workflows use the Discord server.'],
['What is Bedrock Edition?','Bedrock is Minecraft’s cross-platform edition. This project focuses on its mobile community and Bedrock-specific content formats.'],
['What are Add-Ons?','Add-Ons are Bedrock content that can customize gameplay and world content. Microsoft documents examples including blocks, items, mobs and recipes.'],
['What is a Resource Pack?','A Resource Pack supplies assets such as textures, sounds, models and other visual resources.'],
['What is a Behavior Pack?','A Behavior Pack defines gameplay behavior such as entity behavior, loot, spawning, recipes and related systems.'],
['Can Java mods be installed into Bedrock?','Not directly. Java Edition mods and Bedrock Add-Ons/pack systems are different technologies, so the directory keeps those categories separate.'],
['What does HT1 mean?','HT means High Tier. In a five-level tier family, HT1 is the highest high-tier placement used by this community’s chosen ruleset.'],
['What does HT2 mean?','HT2 is a very strong competitive placement below HT1 in the five-level explanation used on this page.'],
['What does LT3 mean?','LT means Low Tier. LT3 is the lower side of Tier 3; it is a competitive placement, not a statement about a player’s overall worth.'],
['What does LT4 mean?','LT4 is the lower side of Tier 4. Placement depends on the testing ruleset used by staff.'],
['What does LT5 mean?','LT5 is the lowest band in this five-level tier family. It is still a real published placement if staff assign it.'],
['Does the website automatically rank players?','No. Player records are published after the community’s human application/review process.'],
['How do I apply for the tier list?','Use the Tier List Application button. It opens the configured Google Form.'],
['Can I choose my own tier?','You can describe your experience, but the final published placement is not self-assigned.'],
['Can I retest?','That depends on the community review process and staff. Use Discord for the current testing rules.'],
['Where do community videos come from?','Featured creator videos are retrieved from YouTube’s public API when the site owner configures a YouTube Data API key.'],
['Why is @HeyHazik shown?','The starter creator directory contains @HeyHazik as the initial featured channel. More creators can be added as real approved entries.'],
['Are video views or subscribers invented?','No. This site does not display fabricated numbers. If statistics are added later, they should come from the real API.'],
['Can I submit my YouTube channel?','Yes. Sign in and use the community submission form. Submissions are stored as pending and are not automatically published.'],
['Can I submit a project?','Yes. Sign in and submit a real project URL. Staff can review it before publication.'],
['Are projects automatically approved?','No. The submission endpoint records the submission as pending. A real moderation workflow is required before publishing.'],
['Where is my profile picture stored?','It is not stored in the site database. Google provides the picture during sign-in and the browser can use it for the current account UI.'],
['What does this site store?','For signed-in accounts it stores the Google subject identifier, email, chosen name and optional YouTube/Discord fields. It also stores moderation submissions.'],
['Is this an official Minecraft website?','No. It is an independent community project and is not affiliated with Mojang Studios or Microsoft.']
];
function api(path){return `${state.apiBase}${path}`}
async function get(path,opts={}){const r=await fetch(api(path),{credentials:'include',...opts});let data={};try{data=await r.json()}catch{}if(!r.ok)throw new Error(data.error||`Request failed (${r.status})`);return data}
function setLinks(){document.querySelectorAll('[data-discord]').forEach(a=>a.href=state.discord);document.querySelectorAll('[data-tier-form]').forEach(a=>a.href=state.form)}
function renderCategories(){const el=$('#categoryTabs');if(!el)return;el.innerHTML=categories.map(([name])=>`<button class="category-tab ${state.category===name?'active':''}" data-category="${esc(name)}"><i class="fa-solid ${icons[name]}"></i>${esc(name)}</button>`).join('');el.querySelectorAll('[data-category]').forEach(b=>b.onclick=()=>{state.category=b.dataset.category;renderCategories();renderProjects()})}
function projectCard(p){const icon=icons[p.category]||'fa-cube';const tags=(p.tags||[]).slice(0,4).map(t=>`<span>${esc(t)}</span>`).join('');const action=p.url?`<a class="project-link" href="${esc(p.url)}" target="_blank" rel="noopener">Open project <i class="fa-solid fa-arrow-up-right-from-square"></i></a>`:'';return `<article class="project-card"><div class="project-cover"><i class="fa-solid ${icon}"></i></div><div class="project-body"><div class="project-type">${esc(p.category||'Project')}</div><h3>${esc(p.name||'Untitled')}</h3><p>${esc(p.description||'No description provided.')}</p><div class="project-meta"><span><i class="fa-solid fa-user"></i> ${esc(p.author||'Unknown')}</span>${tags}</div>${action}</div></article>`}
function renderProjects(){const q=($('#projectSearch')?.value||'').trim().toLowerCase();const sort=$('#projectSort')?.value||'name';let list=state.projects.filter(p=>(state.category==='All'||p.category===state.category)&&(!q||[p.name,p.author,p.description,...(p.tags||[])].join(' ').toLowerCase().includes(q)));list.sort((a,b)=>String(a[sort]||'').localeCompare(String(b[sort]||'')));$('#projectGrid').innerHTML=list.length?list.map(projectCard).join(''):`<div class="empty"><i class="fa-solid ${state.projects.length?'fa-filter':'fa-box-open'}"></i><h3>${state.projects.length?'No matching projects':'No published projects yet'}</h3><p>${state.projects.length?'Try another category or search.':'The directory starts empty. Add real projects through moderation before they appear here.'}</p><a href="#community">Submit community content <i class="fa-solid fa-arrow-right"></i></a></div>`}
function renderPlayers(){const table=$('#tierTable');$('#playerCount').textContent=state.players.length;const count=state.players.length; if(!count){table.innerHTML='<div class="empty"><i class="fa-solid fa-ranking-star"></i><h3>No ranked players yet</h3><p>Real player records appear after applications and staff review.</p><a data-tier-form target="_blank" rel="noopener">Apply for tier list <i class="fa-solid fa-arrow-right"></i></a></div>';setLinks();return}table.innerHTML=state.players.map((p,i)=>`<div class="player-row"><div><div class="player-name">${esc(p.name)}</div><div class="player-meta">${esc(p.region||'')}</div></div><div><span class="tier-badge">${esc(p.tier||'—')}</span></div><div class="player-meta">${esc(p.platform||'Bedrock Mobile')}</div><div class="player-meta">${esc(p.mode||'PvP')}</div><div class="player-meta">#${i+1}</div></div>`).join('')}
function renderFaq(){const q=($('#faqSearch')?.value||'').trim().toLowerCase();const list=faqs.filter(([a,b])=>(a+' '+b).toLowerCase().includes(q));$('#faqList').innerHTML=list.map(([question,answer])=>`<div class="faq-item"><button class="faq-question"><span>${esc(question)}</span><span>+</span></button><div class="faq-answer">${esc(answer)}</div></div>`).join('')||'<div class="empty">No FAQ matches.</div>';$('#faqList').querySelectorAll('.faq-question').forEach(btn=>btn.onclick=()=>{const item=btn.parentElement;item.classList.toggle('open');btn.lastElementChild.textContent=item.classList.contains('open')?'−':'+'})}
function renderCreators(creators){const rail=$('#creatorRail');if(!creators?.length){rail.innerHTML='<div class="empty">No creators have been approved yet.</div>';return}rail.innerHTML=creators.map(c=>`<article class="creator-card"><div class="creator-avatar fallback"><i class="fa-brands fa-youtube"></i></div><div><b>${esc(c.name||c.handle)}</b><span>${esc(c.handle)}</span><a href="${esc(c.channelUrl)}" target="_blank" rel="noopener">Open channel <i class="fa-solid fa-arrow-up-right-from-square"></i></a></div></article>`).join('')}
function renderVideos(data){const area=$('#videoArea');if(!data?.enabled){area.innerHTML='<div class="empty"><i class="fa-brands fa-youtube"></i><h3>YouTube integration is not configured</h3><p>Add YOUTUBE_API_KEY on the backend to load real videos.</p></div>';return}if(data.error){area.innerHTML=`<div class="empty"><i class="fa-solid fa-triangle-exclamation"></i><h3>Could not load YouTube</h3><p>${esc(data.error)}</p></div>`;return}const ch=data.channel;area.innerHTML=`<div class="video-header"><h3><i class="fa-brands fa-youtube"></i> Latest from ${esc(ch.title)}</h3><span>${esc(ch.handle)}</span></div><div class="video-rail">${(data.videos||[]).map(v=>`<a class="video-card" href="${esc(v.url)}" target="_blank" rel="noopener"><img loading="lazy" src="${esc(v.thumbnail)}" alt=""><div class="video-body"><h4>${esc(v.title)}</h4><span>${new Date(v.publishedAt).toLocaleDateString()}</span></div></a>`).join('')||'<div class="empty">No public videos were returned by YouTube.</div>'}</div>`}
function showToast(text){const t=$('#toast');t.textContent=text;t.classList.add('show');clearTimeout(showToast.t);showToast.t=setTimeout(()=>t.classList.remove('show'),3200)}
function openModal(view){$('#authView').innerHTML=view;$('#authModal').classList.add('show');$('#authModal').setAttribute('aria-hidden','false')}
function closeModal(){$('#authModal').classList.remove('show');$('#authModal').setAttribute('aria-hidden','true')}
function authTitle(){return state.user?`<div class="auth-title">Welcome, ${esc(state.user.name)}</div><p class="auth-copy">Your community profile is active.</p>`:`<div class="auth-title">Sign in to Minecraft Mobile</div><p class="auth-copy">Browsing is open to everyone. Sign in only when you want to submit community content or keep a profile.</p><div class="google-slot" id="googleButton"></div>`}
async function loadGoogleScript(){if(window.google?.accounts?.id)return;await new Promise((resolve,reject)=>{const s=document.createElement('script');s.src='https://accounts.google.com/gsi/client';s.async=true;s.onload=resolve;s.onerror=reject;document.head.appendChild(s)})}
function googleCallback(response){get('/api/auth/google',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({credential:response.credential})}).then(data=>{state.googleProfile=data.googleProfile||null;if(data.user){state.user=data.user;renderAccount();openProfile(false)}else{openProfile(true)}}).catch(e=>showToast(e.message))}
async function openAccount(){if(state.user){openProfile(false);return}openModal(authTitle());if(!state.googleClientId){$('#googleButton').innerHTML='<div class="submission-note">Google sign-in is not configured yet. Add GOOGLE_CLIENT_ID and SESSION_SECRET on the backend.</div>';return}try{await loadGoogleScript();google.accounts.id.initialize({client_id:state.googleClientId,callback:googleCallback,auto_select:false});google.accounts.id.renderButton($('#googleButton'),{theme:'filled_black',size:'large',shape:'rectangular',text:'signin_with',width:330})}catch(e){$('#googleButton').innerHTML='<div class="submission-note">Google Identity Services could not load. Check the network and OAuth configuration.</div>'}}
function openProfile(first){const gp=state.googleProfile;const user=state.user;const name=esc(user?.name||gp?.name||gp?.email?.split('@')[0]||'');const picture=gp?.picture?`<img src="${esc(gp.picture)}" alt="">`:'<div class="fallback"><i class="fa-regular fa-user"></i></div>';openModal(`<div class="auth-title">${first?'Finish your profile':'Your profile'}</div><p class="auth-copy">${first?'Your Google account is connected. Choose the name the community should see.':'Update the optional community details below.'}</p><div class="profile-preview">${picture}<div><b>${esc(gp?.email||user?.email||'Google account')}</b><span class="submission-note">Profile photo is not stored by this site.</span></div></div><form id="profileForm"><div class="form-field"><label>COMMUNITY NAME</label><input name="name" maxlength="40" required value="${name}"></div><div class="form-field"><label>YOUTUBE CHANNEL (OPTIONAL)</label><input name="youtube" maxlength="120" placeholder="@YourChannel" value="${esc(user?.youtube||'')}"></div><div class="form-field"><label>DISCORD (OPTIONAL)</label><input name="discord" maxlength="80" placeholder="username or server profile" value="${esc(user?.discord||'')}"></div><div class="modal-actions"><button type="button" class="button secondary" data-close-modal>Cancel</button><button class="button primary">Save profile</button></div></form>`);$('#profileForm').onsubmit=saveProfile}
async function saveProfile(e){e.preventDefault();const f=new FormData(e.currentTarget);try{const data=await get('/api/auth/profile',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:f.get('name'),youtube:f.get('youtube'),discord:f.get('discord')})});state.user=data.user;state.googleProfile=null;renderAccount();closeModal();showToast('Profile saved.')}catch(err){showToast(err.message)}}
function renderAccount(){const b=$('#accountButton');if(!b)return;if(state.user)b.innerHTML='<i class="fa-regular fa-circle-user"></i> '+esc(state.user.name);else b.innerHTML='<i class="fa-regular fa-user"></i> Sign in'}
async function submitCommunity(){if(!state.user){showToast('Sign in is only needed to submit community content.');openAccount();return}openModal(`<div class="auth-title">Submit to the community</div><p class="auth-copy">Submissions are stored as pending and are not automatically published.</p><form class="submission-form" id="submissionForm"><select name="type"><option value="youtube">YouTube channel/video</option><option value="project">Bedrock project</option></select><input name="url" type="url" required placeholder="https://…"><textarea name="note" maxlength="500" placeholder="Tell staff what this is (optional)"></textarea><div class="submission-note">Only submit content you own or have permission to share.</div><div class="modal-actions"><button type="button" class="button secondary" data-close-modal>Cancel</button><button class="button primary">Submit for review</button></div></form>`);$('#submissionForm').onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget);try{const d=await get('/api/community/submit',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type:f.get('type'),url:f.get('url'),note:f.get('note')})});closeModal();showToast(d.message)}catch(err){showToast(err.message)}}}
async function boot(){const runtime=window.MM_CONFIG||{};state.apiBase=runtime.apiBase||'';try{const cfg=await get('/api/config');state.discord=cfg.discord;state.form=cfg.tierApplication;state.googleClientId=runtime.googleClientId||cfg.googleClientId||'';if(cfg.apiBase&&!state.apiBase)state.apiBase=cfg.apiBase}catch(e){state.googleClientId=runtime.googleClientId||''}setLinks();renderCategories();renderFaq();renderAccount();$('#menuButton')?.addEventListener('click',()=>$('#mainNav').classList.toggle('open'));$('#mainNav')?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>$('#mainNav').classList.remove('open')));$('#projectSearch')?.addEventListener('input',renderProjects);$('#projectSort')?.addEventListener('change',renderProjects);$('#faqSearch')?.addEventListener('input',renderFaq);$('#accountButton')?.addEventListener('click',openAccount);$('#submitCommunity')?.addEventListener('click',submitCommunity);document.addEventListener('click',e=>{if(e.target.matches('[data-close-modal]'))closeModal()});$('#authModal')?.addEventListener('click',e=>{if(e.target.id==='authModal')closeModal()});try{const [projects,players,stats,me,creators,youtube]=await Promise.all([get('/api/projects'),get('/api/players'),get('/api/stats'),get('/api/auth/me'),get('/api/creators'),get('/api/youtube').catch(()=>null)]);state.projects=projects;state.players=players;state.user=me.user;$('#projectCount').textContent=stats.projects;$('#creatorCount').textContent=creators.length;renderProjects();renderPlayers();renderCreators(creators);renderVideos(youtube);renderAccount()}catch(e){console.error(e);renderProjects();renderPlayers();renderCreators([{handle:'@HeyHazik',name:'HeyHazik',channelUrl:'https://www.youtube.com/@HeyHazik'}]);renderVideos(null)}setLinks()}
boot();
